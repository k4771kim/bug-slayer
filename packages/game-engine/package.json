{
  "name": "@bug-slayer/game-engine",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "scripts": {
    "build": "rm -rf dist && tsc --incremental false && mkdir -p dist/data && cp data/*.json dist/data/",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@bug-slayer/shared": "workspace:*",
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
